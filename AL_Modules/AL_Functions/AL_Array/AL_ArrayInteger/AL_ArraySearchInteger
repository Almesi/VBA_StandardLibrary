Function AL_ArraySearchInteger(ByRef Array1() As Integer, ByVal SearchValue As Integer) As Long

    For i = 0 To UBound(Array1)
        If Array1(i) = SearchValue Then
            AL_Array1SearchInteger = i
            Exit Function
        End If
    Next

End Function